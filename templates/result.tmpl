{{ define "result" }}
    {{ template "header" "/res/css/bordered_search.css" }}
            <form action="/search">
                <input type="text" name="q" placeholder="Search.." value={{ .Query }}>
            </form>
            {{ range .Result }}
                <p>{{ . }}</p>
            {{ end }}
    {{ template "footer" }}
{{ end }}